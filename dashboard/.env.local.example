NEXT_PUBLIC_DEMO_MODE=0

# Copy to .env.local for local dev. Values come from Terraform outputs:
#   terraform -chdir=../infra output -raw cosmos_endpoint
#   terraform -chdir=../infra output -raw cosmos_primary_key
COSMOS_ENDPOINT=https://cosmos-mktstream-dev-lhc7i.documents.azure.com:443/
COSMOS_KEY=your-cosmos-primary-key
COSMOS_DATABASE=market
